/* eslint-disable @typescript-eslint/no-unused-vars */
import { MantineThemeOverride } from '@mantine/core'

export const themeOverrides = (baseTheme: MantineThemeOverride): Partial<MantineThemeOverride> => ({})
